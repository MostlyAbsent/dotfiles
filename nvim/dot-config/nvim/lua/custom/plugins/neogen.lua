-- neogen
--
-- Create annotations with one keybind, and jump your cursor in the inserted
-- annotation
-- Defaults for multiple languages and annotation conventions
-- Extremely customizable and extensible Written in lua (and uses Tree-sitter)

-- COMMENT : Autogenerate doc-comments. I installed it for java, it
-- auto-detects params etc.

return {
  'danymat/neogen',
  config = true,
  -- Uncomment next line if you want to follow only stable versions
  version = '*',
}
