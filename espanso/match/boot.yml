# espanso match file

# Boot related notes

matches:
  - trigger: "\\boot\\e"
    label: External Boot
    replace: Started the machine from external media for diagnostics.

  - trigger: "\\boot\\n"
    label: Normal Boot
    replace: Started the machine normally.
